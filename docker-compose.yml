version: "3.8"
services:
  freellmapi:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - PORT=3001
      - HOST=0.0.0.0
