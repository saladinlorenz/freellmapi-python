## Objective
- Produce an exhaustive technical specification (RESEARCH ONLY, no code) of the TypeScript repo at `E:\freellmapi-main\freellmapi-main` sufficient to reimplement it in Python without access to original code.
- Spec must contain 15 sections: crypto, config/env vars, complete SQLite DDL, migration runner mechanics, error classification, sampling params, tool call rescue, structured output, think tags, content normalization, image normalize, client classifier, remaining lib modules, admin routes summary, index.ts startup sequence — with exact constants/formats.

## Important Details
### Newly completed since last checkpoint
- **middleware/requireAuth.ts** (18 lines): token from `Authorization: Bearer` (regex strip) or `x-dashboard-token`; validateSession ? req.user; 401 `{error:{message:'Authentication required', type:'authentication_error'}}`.
- **middleware/errorHandler.ts** (50 lines): logs `[Error]` (message only in production); headersSent ? next; body-parser `entity.too.large` or status 413 ? message "Request body too large (<received> bytes) for the <limit>-byte limit. Vision requests embed base64 images in the body; raise REQUEST_BODY_LIMIT_MB (default 25) to accept larger payloads.", logs analytics row via logRequest('proxy','payload-too-large',null,'error') for /v1|/v1beta|/mcp paths, responds 413 `{type:'invalid_request_error', code:'request_too_large'}`; otherwise status = err.status ?? 500, message = 'Internal server error' when prod && =500 else err.message, type = err.name ?? 'server_error'.
- **middleware/rateLimit.ts** (145 lines): two identical fixed-window per-IP limiters; WINDOW_MS=60_000; MAX_TRACKED_IPS=10_000 (prune expired when exceeded); ip = req.ip ?? socket.remoteAddress ?? 'unknown'; headers X-RateLimit-Limit/Remaining/Reset (reset = ceil(resetAt/1000)); over-limit ? Retry-After ceil + 429 `{message:"Rate limit exceeded: more than <limit> requests per minute. Retry in <n>s.", type:'rate_limit_error'}`; PROXY default 120 RPM (PROXY_RATE_LIMIT_RPM, floor, invalid/negative?default); ADMIN default 600 RPM (ADMIN_RATE_LIMIT_RPM); 0 disables.
- **db/index.ts** (251 lines, COMPLETE): DEFAULT DB path `<server>/../../data/freeapi.db` resolved from module dir; getDefaultDbPath = FREEAPI_DB_PATH trim || default; factory = nodeSqliteFactory on android else better-sqlite3 (helpful reinstall error mentioning Node 22.13+ for Termux); connectDb: mkdir data dir (unless ensureDir=false/:memory:), harden dir BEFORE connect (Windows ACL inheritance), pragmas `journal_mode=WAL` (not :memory:), `foreign_keys=ON`, `busy_timeout=5000`, then restrictAllToOwner(db, -wal, -shm); initDb runs migrations unless NODE_ENV==='development' (dev instead checks migrations table exists else exits 1 with "npm run db:migration:up" hint); then initEncryptionKey if needed; getUnifiedApiKey reads settings row; regenerateUnifiedKey mints `freellmapi-` + randomBytes(24) hex; getSetting/setSetting upsert helpers.
- **Data-dir hardening rule** (shouldHardenDataDir): FREEAPI_DB_DIR_HARDENING tri-state override ('true/1/yes', 'false/0/no', else unset); harden if dir was just created OR equals default data dir (case-insensitive resolve on win32); otherwise one-time log advising the env flag.
- **routes/settings.ts** (464 lines, COMPLETE): GET /version {version: getAppVersion()}; update-check GET/PUT (zod strict {enabled}, setting 'update_check_enabled' '1'/'0'); compression GET/PUT via compressionUpdateSchema; unify GET/PUT ({enabled?, overrides?} partial); fusion GET/PUT (savedFusionConfigSchema; response {config, maxK:getFusionMaxK()}); anthropic-map/gemini-map GET/PUT (map of opus/sonnet/haiku/default ? 'auto'|model_id, errors from thrown zod mapped to 400); agent-compatibility GET/PUT (ollamaEmulation enum off|open-loopback|key-required stored 'ollama_emulation'; exposeClaudeDiscoveryAliases ? setting 'expose_cc_discovery_aliases'); url-tokens GET/POST(label =120 optional)/DELETE(:id int>0, 404 'Active URL token not found', 204); output-limit GET/PUT (mode 'off'|'auto'|int=1; state {mode, effectiveCap, autoValue:UNIFIED_MAX_TOKENS_AUTO}); guardrails GET/PUT (requestMaxTokensBudget/maxConsecutiveUpstreamFails ints =0, settings keys from guardrails.ts); headroom GET/PUT (rampStart/floor decimals 0..1 nullable ? setHeadroomThresholds, null=scoring default); api-key GET + POST /api-key/regenerate; proxy GET/PUT (partial proxyUrl/enabled/bypassPlatforms; URL validated against PROXY_SCHEMES list http,https,socks5,socks5h,socks4,socks4a; settings keys proxy_url/proxy_enabled/proxy_bypass CSV; live apply* calls); POST /proxy/test — probe WITHOUT saving, draft proxyUrl or saved fallback; target selection: PROXY_TEST_URL env ? first enabled non-bypassed platform's provider modelsUrl ? bypassed ones ? skip 'custom' ? DEFAULT_PROXY_PROBE_TARGET; probeProxyUrl never persists.
- **routes/keys.ts** (1460 lines, COMPLETE): PLATFORMS const = google, groq, cerebras, bai, nvidia, mistral, openrouter, github, cohere, cloudflare, zhipu, ollama, kilo, pollinations, llm7, huggingface, opencode, ovh, agnes, reka, siliconflow, routeway, bazaarlink, ainative, aion, anyapi, requesty, navy, nara, sealion, orcarouter, unorouter, xkiro, modelscope, qianfan, volcengine, longcat, xfyun, aihorde, custom (Moonshot/MiniMax dropped V4, HuggingFace re-added V13, SambaNova dropped V23); upload multer memoryStorage =5MB/file =10 files ext whitelist .env/.json/.jsonc/.md/.txt/.csv; addKeySchema {platform enum, key?, label?, proxyUrl?}; updateKeySchema requires =1 of enabled/label/modelScope(=100 ids each 1–200 chars nullable)/proxyUrl; keyless providers store 'no-key' sentinel and re-enable existing row instead of duplicating; export isExportableKey: custom?has baseUrl else real value ? ''/'no-key'; export formats: env (unique names `<PLATFORM>_KEY`, `_2` suffixes; custom ? indexed pairs CUSTOM_<n>_BASE_URL/CUSTOM_<n>_KEY with comment label; blank-line separated), csv (`platform,key,label,base_url` header, quote-doubling, formula-injection neutralize prefix `'` for =+-@tabCR on labels only), json {version:1, exportedAt ISO, source:'freellmapi', keys:[{platform,key,label,baseUrl?}]}; both /export and /:id/reveal require x-reauth-password verified against dashboard password (403 authentication_error) EXCEPT desktop build (FREEAPI_DESKTOP==='1') from loopback socket address (::1 or 127.x after ::ffff: strip) (#786); healthy=true filters status='healthy'; reveal decrypt failure ? 500 "stored with a different ENCRYPTION_KEY"; add returns maskedKey/maskedProxyUrl/modelsAvailable/notice (catalog-tier empty notice text captured verbatim); custom endpoint machinery: resolveEndpointRef(keyId stronger than baseUrl; baseUrl normalized; first decryptable key of pool; throws {status,message}); discover-models (GET upstream /models via discoverEndpointModels, marks registered per ENDPOINT pool, nothing written); probe (one minimal chat request; success inserts requests row mirroring proxy shape + clears cooldowns + positive supports_tools write-back scoped to endpoint pool #874; failures record NOTHING); POST /custom (model/models union entries; dedupe by id; top-level displayName binds only to single unnamed entry #704; credential-only add path #702 with 409 duplicate-key check; registerCustomModels returns created/alreadyRegistered counts); import (multer single 'file'), preview (array files =10, duplicate detection vs decrypted existing set, echoes baseUrl+models), import-selected (=100 keys; duplicates counted skipped; SSRF verdicts cached per base_url); delete cascade: custom key with sibling endpoint key ? models/embedding_models/media_models.key_id reassigned then row deleted; last key of platform custom ? wipe all custom rows + their fallback_config + retarget embeddings_default_family to first remaining family; tombstones recorded before delete (#926 anti-resurrection); PATCH /platform/:platform toggles whole platform; PATCH /:id applies enabled/label/proxyUrl(re-encrypt trio)/modelScope(dedupe, empty?NULL).
- **routes/analytics.ts** (784 lines, COMPLETE): ENDPOINT_ID_SQL = `COALESCE(rtrim(trim(k.base_url), '/'), '')` joined via LEFT JOIN api_keys k ON k.id=r.key_id; since windows 24h/7d(default)/30d/90d as SQLite 'YYYY-MM-DD HH:MM:SS'; /summary reads request_hourly aggregate (hour = since.slice(0,13)+':00:00') for totals/successRate(success/(success+error), canceled excluded)/tokens/lifetime first_request_at; raw rows only for avgLatencyMs, est savings (input*paid_input_per_m + output*paid_output_per_m /1e6 COALESCE FALLBACK_INPUT_PER_M/FALLBACK_OUTPUT_PER_M, success rows only), pinned/pinHonored, p50/p95 nearest-rank ORDER BY latency LIMIT 1 OFFSET floor((count-1)*f) over non-null latencies, avgTtfbMs, requestTypeCounts {chat,embedding}; lifetimeTotalRequests from settings total_requests; /by-model groups (platform, endpoint, model_id) joining models on +endpoint_scope (row multiplication bug note #889/#651), emits providerIdFor/endpoint display name, successRate NULL-safe, est_cost; /by-platform same grouping + per-group p95 via prepared stmt + avg tokens/sec `output_tokens / (latency_ms/1000)` over positive rows; /by-client groups client_agent with strftime last_seen_at; /by-key TOP 50 by requests LEFT JOIN api_keys (deleted key keeps row null label); /timeline from request_hourly, interval hour|day (default hour iff range=24h), strftime formats '%Y-%m-%dT%H:00:00' | '%Y-%m-%d', tzOffset minutes clamp [-720,840] bound param modifier '+N minutes'; /error-distribution CASE buckets on error LIKE patterns: 429/rate limit/too many/quota ? 'Rate Limited (429)'; 401/unauthorized/invalid.*key ? 'Auth Error (401)'; 403/forbidden; 404/not found; timeout/ETIMEDOUT/ECONNREFUSED ? 'Timeout/Connection'; 500/internal server; 503/unavailable; ELSE 'Other' — returns byCategory, byPlatform(endpoint-scoped), detailed(platform,model_id,category,count); /errors latest 50 joined endpoint; /requests paged (limit clamp 1–500 default 100, offset =0), status filter whitelist, provider filter grammar: 'custom' = orphan bucket (platform='custom' AND endpoint=''), 'custom:<base_url>' normalized equality, /^[A-Za-z0-9_-]{1,64}$/ slug; legacy platform param = plain platform match; includes client_ip/client_user_agent/client_agent/key_label/attempt_count subquery; /requests/:id detail + request_attempts ladder (ordinal, key_ordinal anonymized, outcome, start_offset_ms, duration_ms, error_summary).
- **routes/fallback.ts** (668 lines, COMPLETE): GET /routing spreads getRoutingScores minus peakHours plus peakHoursAdjust/startHour/endHour/timezone; penalty-inspector passthrough; PUT /routing zod {strategy enum priority|balanced|smartest|fastest|reliable|custom, weights{reliability,speed,intelligence =0} optional (renormalized; all-zero throws?400), exploreEnabled?, peakHoursAdjust?, peakStartHour/EndHour 0–23 ints, peakTimezone IANA validated, keySelectionStrategy auto|least-remaining}; weights persisted BEFORE strategy flip; response echoes presets + active weights + peakAdjusted flag; MODEL_COLUMNS/MODEL_JOINS shared SQL (joins api_keys label, model_overrides has_overrides/overrides_json, catalog_model_tombstones kind='chat'); PROFILE_CHAIN_SQL = full catalog LEFT JOIN profile_models(pm)+fallback_config(fc); GLOBAL_CHAIN_SQL = fallback_config JOIN models; orderProfileRows: chain rows first by chain_priority then db id, rest appended in catalog priority order numbered maxStored+1+i with enabled=0 (nothing persisted on GET #1021); GET / row shape: effectivePriority=priority+penalty, rateLimitHits, monthlyTokenBudgetTokens=parseBudget×max(1,healthyKeys(platform)) (#456 unified key count enabled+status IN healthy/unknown), groupKey/canonicalId/groupLabel, qualifiedModelId, overrideFields, retiredUpstream=tombstone_source==='upstream_eol'+retiredReason, keyCount, source='custom' iff platform custom or key_id not null; PUT / = array [{modelDbId,priority,enabled}] full replace: profile mode UPSERTs (ON CONFLICT(profile_id,model_db_id)) skipping unknown ids; global mode UPDATE-only; POST /sort/:preset intelligence|speed|budget (budget score = tpd_limit*30 else parse biggest K/M/B number × multiplier, unlimited/8=Infinity; INTELLIGENCE_TIER CASE size_label Frontier/Large/Medium/Small?tier ASC then rank ASC); sort writes priorities only, never enables (profile upsert enabled=0 for new rows; global preserves flags differently — profile sort helper preserves existing enabled map, defaults new=1 in profiles.ts variant); /token-usage: month-to-date SUM(input+output) per (platform,model_id) WHERE request_type='chat', budget × healthy keys, totalBudget sums ALL models incl disabled; /rate-limit-usage single grouped scan of rate_limit_usage (kind='request' counts minute/day, kind='tokens' sums minute tokens) over sliding minute/day; per model picks ELIGIBLE key with LOWEST pressure (max of used/limit ratios across rpm/rpd/tpm windows; eligibility enabled+healthy/unknown+scopeAllows+custom endpoint base_url match) — mirrors router's selectKeyForModel; no eligible key ? nulls.
- **routes/profiles.ts** (518 lines, COMPLETE): RESERVED_PROFILE_NAMES auto/smart/fast/cheap/budget/intelligence/speed/active/default; name schema 1–20 chars ^[a-zA-Z0-9-_]+$ case-insensitive reserved check (409 dupes); create {emoji =4 default '', color default '#6366f1', sourceProfileId?, empty:false}; empty wins over source; clone copies layout_config+auto_sort; no source ? copyFromDefault (fallback_config ordered copy); GET ordering: default type DESC, is_favorite DESC, sort_order ASC, id ASC; protected (default/builtin) blocks name/emoji/color edits and deletion (also cannot delete last profile; deleting active switches to first default/builtin else lowest sort_order); reorder = DELETE all + INSERT transaction; reset writes canonical baseline layout_config JSON (viewMode list, compactMode false, limitsMode false, limitsVariant circle, kanban/tier arrays with board-default/tier-default rgb(99,102,241)); auto_sort preset physically sorts (intelligence/speed/budget; budget score fn duplicated here); seedProfiles: Default profile ? '#6366f1' sort -1, scores display_name contains gpt-4o?3, qwen3-coder?2, gemini?1 sorted desc then name asc, all enabled=1, sets active_profile_id.
- **routes/premium.ts** (126 lines, COMPLETE): maskKey first7…last4 (>10 chars); statusPayload {hasKey, maskedKey, license: getCachedLicenseStatus(), catalog: getSyncState(), siteUrl: PREMIUM_SITE_URL ?? 'https://freellmapi.co' trailing-slash-stripped}; POST /key validates =8 chars locally then POST {key} to catalogBaseUrl()/v1/license/activate 15s timeout; invalid reasons map unknown_key/expired/canceled/refunded ? friendly texts; valid ? setSetting(SETTING_LICENSE_KEY) + refreshLicenseStatus + syncCatalog(true); DELETE /key deletes license key+status settings, fire-and-forget sync back to free tier; POST /sync manual refresh+sync; POST /portal POSTs to /v1/portal returning Stripe portal {url}.
- **routes/conversations.ts** (263 lines, COMPLETE): playground_conversations table; MAX_TITLE_LEN=200, MAX_MESSAGES_BYTES=2MB (413 type conversation_too_large with KB math); message schema role user|assistant, content string, images? string[], isError?, reasoning?, meta{platform?,model?,latency?,fallbackAttempts?,fusionPanel[{platform,model,status ok|failed,content,error}],fusionJudge{platform,model}|null}; `streaming` deliberately stripped (finished transcript); model =200 nullable, systemPrompt =32000 nullable; strict schemas; GET / sidebar uses json_array_length(messages_json) AS message_count ordered updated_at_ms DESC; PUT = partial upsert (absent fields keep stored values) single UPDATE; corrupted messages_json parses to [] rather than 500.
- **routes/update.ts** (618 lines, COMPLETE): REPOSITORY 'tashfeenahmed/freellmapi'; CACHE_TTL_MS=5min, RELEASE_CACHE_TTL_MS=6h, FAILURE_TTL_MS=45s, MAX_RELEASE_BODY_LENGTH=20000, MAX_TAG_LENGTH=64, SHA_PATTERN ^[0-9a-f]{40}$i, COMPARE_STATUSES identical|ahead|behind|diverged ? statuses current|available|ahead(diverged naming quirk: 'behind' maps to CheckStatus 'ahead')|diverged, MAX_CHANGES=8, MAX_CHANGE_MESSAGE_LENGTH=160 (first line only), MAX_ATOM_FEED_LENGTH=1MB capped stream read; identity resolution FREELLMAPI_COMMIT_SHA env ? FREELLMAPI_INSTALL_METHOD docker/desktop (before SHA check) ? async git rev-parse HEAD 5s timeout ? unknown; GitHub API compare <sha>...main Accept vnd.github+json UA freellmapi-update-checker X-GitHub-Api-Version 2022-11-28 optional Bearer FREELLMAPI_UPDATE_GITHUB_TOKEN; 404?unknown; 401/403/429 ? Atom feed fallback parser (entry regexes Commit/<sha40>, title decodeXmlText entities amp apos gt lt quot + numeric refs); readCapped enforces content-length AND streaming byte ceiling; GET /release gated by FREELLMAPI_UPDATE_CHECK==='off' OR setting update_check_enabled!=='1' ? {disabled:true} (CSP connect-src 'self' rationale); GET /check ? disabled/unsupported(no sha)/result or 502 upstream_error; GET /status ? cached status or idle.
- **routes/models.ts** (283 lines, COMPLETE): modelUpdateSchema strict with displayName 1–200, ranks 1–1000, sizeLabel =40 ('' legal = unscored), limits nullable positive, monthlyTokenBudget =80, contextWindow nullable positive, booleans, fallbackEnabled separate; PATCH writes models columns + mirrors patch into model_overrides when isCatalogManagedModel; disabling cascades profile_models.enabled=0 + pruneUnavailableSavedFusionConfig + fallback_config.enabled sync (+active profile row); DELETE records catalog_model_tombstone (managed) or custom-model tombstone (custom scope), removes fallback_config row, deleteUnusedCustomEndpointKey; GET / lists with COALESCE(profile/fallback priority) join overrides+keys; source mapped 'user'?'custom' else 'catalog'.
- **routes/embeddings.ts** (283 lines, COMPLETE): GET / families grouped from listEmbeddingModels with defaultFamily, per-provider keyCount (custom ? its own key usable 1/0); POST /custom {baseUrl url, model min1, displayName?, family?(default=modelId), apiKey?, label?, quotaLabel?(default 'custom endpoint'), maxInputTokens?} probes dimensions FIRST (probeEmbeddingDimensions; EmbeddingsError.status honored) then transactional resolveCustomEndpointKey(additional credential #619) + registerCustomEmbeddingModel (family-dimension conflict aborts leaving no half-row); PUT / {defaultFamily?(must exist), providers[] priority/enabled bulk}; DELETE /custom/:id removes + reaps unused endpoint key + retargets default family to first remaining (ORDER BY family, priority); GET /usage per family requests_today (start of day) + tokens_month (start of month) joined request_type='embedding' status='success', NO budget denominator (quota_label free-text honesty), representative provider = highest-priority enabled row per family.
- **routes/media.ts** (236 lines, COMPLETE): GET / flat list w/ modality + quotaLabel + keyCount semantics like embeddings; GET /usage modality enum image|video|audio|transcription, requests today/month only (no tokens — billed per image/character); POST /custom registers image|audio|transcription STT endpoints; upsert keeps existing binding when model already on THIS endpoint's pool else rebinds; priority = existing (same modality) else MAX(priority)+1 per modality; PUT /:id enable toggle; DELETE /custom/:id + reap.
- **routes/mcp.ts** (349 lines, COMPLETE): hand-rolled JSON-RPC MCP server, PROTOCOL_VERSION '2025-06-18' always negotiated (batching removed); auth = extractApiToken + timingSafeStringEqual vs unified key, 401 rpcError(id,-32001,'Invalid API key. Authenticate with the unified key as a Bearer token.') echoing body id; batch Array ? 400 -32600; notification = id===undefined (NOT method-prefix detection) ? 202 empty; methods initialize(serverInfo name freellmapi version 1.0.0, capabilities tools), ping, tools/list, tools/call (Object.hasOwn guard vs prototype pollution; tool failure = isError result not protocol error); unknown ? -32601; 7 TOOLS: list_models(available_only default true; supported_parameters via supportedParametersForPlatforms), provider_health(per-platform keys by status + active cooldowns + available models EXISTS subquery honoring m.key_id), usage_summary(range 24h|7d|30d hourly aggregate + top 5 models; success_rate over success+error ×1000 round /10), routing_info(top 10 enabled scores rounded 3dp), set_routing_strategy(validates enum), cache_stats(getCacheStats), compression_stats(getCompressionStats); GET/DELETE / ? 405 stateless notices (-32000).
- **routes/ollama.ts** (607 lines, COMPLETE): emulation modes off/open-loopback/key-required (setting 'ollama_emulation'); isLoopback checks socket.remoteAddress (strip ::ffff:) AND rejects x-forwarded-for first hop that isn't loopback (anti proxy-widening); authorize honors req.urlTokenAuthenticated (URL-token path); normalizeOllamaModel strips :latest, empty?'auto'; done_reason length|stop only; CATALOG_MODIFIED_AT = boot time; /api/tags advertises auto + available models (details.format 'freellmapi', family ownedBy, parameter_size/quantization_level 'remote'); /api/version hardcoded '0.9.9' plain semver; /api/show num_ctx contextWindow ?? 128000, capabilities completion(+tools); /api/chat: empty messages = load/unload probe answered instantly (done_reason load/unload via keep_alive===0) without upstream call; options.num_predict?maxTokens, temperature/top_p/top_k/stop[] mapping; format 'json'?json_object, object?json_schema name 'ollama_response'; session from x-ollama-session-id|x-session-id; NDJSON streaming frames {model,created_at,message:{role,content[,thinking][,tool_calls]},done} + terminal frame done_reason + fabricated nanosecond durations (totalNs from performance.now ×1e6, prompt share ×0.2, min 1ns) + prompt_eval_count/eval_count; tool_calls arguments parsed JSON else {value:str}; message conversion FIFO pendingCalls matching tool_name?tool_call_id, images array ? data:image/png;base64 wrapping ? image_url content blocks; /api/generate prompt+system(+suffix "Complete the text before this suffix:\n") with response/thinking/context:[] wire variants; /api/embed input|prompt ? runEmbeddings(model stripped :latest, inputs[], dimensions) returns embeddings[]+prompt_eval_count; legacy /api/embeddings falls through to dashboard router when bearer is a VALID dashboard session, else 401 authentication_error when token present and mode?key-required, else handled as Ollama legacy ({embedding: vectors[0]}).
- **routes/gemini.ts** (233 lines, COMPLETE): auth extractApiToken ?? ?key= query (console.warn leak warning); errors Gemini-shaped {error:{code,status:'INVALID_ARGUMENT'|'UNAUTHENTICATED'|'NOT_FOUND'}}; GET /v1beta/models (+?available=true filter) and regex GET /models/(.+) (strips models/ prefix, auto allowed); modelShape: inputTokenLimit=contextWindow??autoContextWindow??128000, outputTokenLimit=min(8192,context), supportedGenerationMethods [generateContent, streamGenerateContent, countTokens]; POST regexes :generateContent/:streamGenerateContent/countTokens via actionModel slicing req.path; handleGenerate maps generationConfig (maxOutputTokens DEFAULT 8192 because Gemini CLI sends none — shared 1024 default would truncate), temperature/topP/topK/stopSequences, toolChoice only when tools present, responseFormat via geminiResponseFormat, reasoningEffort via effortFromGeminiThinking(thinkingConfig.thinkingBudget); session header x-goog-request-id|x-session-id; stream alt=sse ? SSE 'data: …\n\n' frames else JSON array streaming ('[' then ','-joined chunks then '\n]'); delta frames candidates[0].content.parts[{text}|{text,thought:true}], finish frame finishReason+usageMetadata(promptTokenCount,candidatesTokenCount,totalTokenCount)+modelVersion; countTokens ? estimateGeminiTokens(body).
- **services/model-listing.ts** (109 lines, COMPLETE): shared behind OpenAI + Anthropic GET /v1/models; NormalizedModel{id,name,ownedBy,available,enabled,contextWindow,intel,platforms[],supportsTools}; availableExpr = enabled=1 AND EXISTS api_keys(enabled=1 AND (key_id IS NULL OR id=key_id)); unify ON ? per group: canonicalId/label, available/enabled some(), contextWindow MAX, intel MIN(rank), platforms unique, supportsTools some(); unify OFF ? ROW_NUMBER PARTITION BY model_id ORDER BY available DESC, intelligence_rank ASC, id ASC; final sort available DESC, enabled DESC, intel ASC, name localeCompare; autoContextWindow = MAX context among AVAILABLE (ignores caller filters), null when none.
- **services/scoring.ts** (476 lines, COMPLETE): convex combination base = Sw·axis ?[0,1] (auto-renormalized) × headroomFactor × rateLimitFactor; RoutingStrategy priority|balanced|smartest|fastest|reliable|custom; KeySelectionStrategy auto|least-remaining (#919); BANDIT_PRESETS balanced {0.5,0.25,0.25}, smartest {0.35,0.1,0.55}, fastest {0.35,0.55,0.1}, reliable {0.7,0.15,0.15} (reliability,speed,intelligence); DEFAULT_STRATEGY balanced; peak-hours: DEFAULT_PEAK_START_HOUR 18, END 6 (spans midnight), TZ UTC, PEAK_SPEED_TO_RELIABILITY 0.6 shifts speed?reliability, EXEMPT strategies fastest+reliable (identity preservation rationale), start==end = EMPTY window not 24h; hourInTimezone via Intl h23, unknown tz ? UTC; reliability = Beta posterior alpha=successes+community.successes+PRIOR_SUCCESS(1), beta likewise; expectedReliability a/(a+ß); speed axis = 0.6×throughputScore + 0.4×ttfbScore; throughputScore = 1 - exp(-tokPerSec/60) (SPEED_SCALE_TOK_S=60 saturating); ttfb ramp 300ms best ? 5000ms zero (TTFB_BEST/WORST); SPEED_PRIOR 0.6 when unmeasured; timeout rows contribute wall-clock latency as 0-token sample + ttfb, capped TIMEOUT_LATENCY_CAP_MS=120_000; observedSpeedRank projects [0,1]?rank 1(worst=10 span 9); intelligence composite = tierValue(Frontier4/Large3/Medium2/Small1 else 0)×1000 - sqrt(max(1,rank))×31 (RANK_SCALE; tier dominance + sqrt-visible edits #673); intelligenceScore min-max normalize, degenerate ? 1; HEADROOM_FLOOR 0.1, HEADROOM_RAMP_START 0.2, shared ramp linear floor+(1-floor)×(remaining/rampStart), operator-tunable clampUnit falls back to defaults on bad input; rateWindowHeadroomFactor same ramp on window utilization; MAX_PENALTY 10, RATE_LIMIT_MAX_DAMP 0.6 ? rateLimitFactor = 1 - (p/10)×0.6 (floor 0.4 score at max penalty); Beta sampler Marsaglia-Tsang two Gamma draws; combineScore renormalizes weights.

## Work State
### Active
- Section 14 (admin routes) is now COMPLETE end-to-end; section 15 startup complete; sections 12–13 lib complete.
- Remaining reads: services core (router.ts 1999 lines — routeRequest/selectKeyForModel/sticky/hedging/headroom wiring; ratelimit.ts 1293 — sliding windows, canMakeRequest, cooldown escalation ladder, learnLimitFromError; health.ts 380; provider-quota.ts 534; catalog-sync.ts 803; declarative-config.ts 470; fusion.ts 724; media service 1045; model-discovery.ts 547; embeddings service 370; backups service 472; cache 330; custom-endpoint 193; model-groups 342; degradation 152; penalty-inspector 230; model-state 301; cooldown-probe 198; custom-model-* small; anthropic-map 143; gemini-map 73; url-tokens 70; quirks 67; profile-models 50; request-retention 144; context-handoff 163; model-weight-overrides 169; model-retirement 108; auth service 113) and providers/ (index 480 registry, base 430 fetchWithTimeout contract, openai-compat 437, google 786, cloudflare/cohere/aihorde/modelscope/zhipu/pollinations adapters) and the three wire routes bodies (proxy.ts, responses.ts, anthropic.ts).

### Blocked
- None.

## Next Move
1. Read services/ratelimit.ts (1293 lines, chunked) — the rate-limit/cooldown engine the spec's routing section depends on.
2. Read services/router.ts (1999 lines, chunked) — routeRequest, sticky sessions, key selection, hedging, headroom wiring, penalties, explore.
3. Read providers/base.ts + providers/index.ts + openai-compat.ts — the provider abstraction and registry every platform adapter builds on.
4. Then the remaining mid-size services in batches, then wire-route bodies as needed for behavior contracts.
5. Assemble the final 15-section specification document once coverage is sufficient.

## Relevant Files
- Middleware (complete): `server\src\middleware\requireAuth.ts`, `rateLimit.ts`, `errorHandler.ts`.
- DB shell (complete): `server\src\db\index.ts`.
- Routes (ALL now read completely): settings.ts, keys.ts, analytics.ts, fallback.ts, profiles.ts, premium.ts, conversations.ts, update.ts, models.ts, embeddings.ts, media.ts, mcp.ts, ollama.ts, gemini.ts, auth.ts, docs.ts, url-tokens.ts, cache.ts, compression.ts, health.ts, backups.ts, logs.ts, client-profiles.ts, status.ts, free-tier.ts.
- Services newly complete: `services\model-listing.ts`, `services\scoring.ts`.
- Previously completed: all 47 lib modules, index.ts, app.ts, full baseline migration + 24 incremental migrations + migration runner, earlier small routes.
- Unread areas: `server\src\services\` (all except scoring/model-listing), `server\src\providers\*`, `server\src\routes\proxy.ts`, `responses.ts`, `anthropic.ts` (bodies), `shared\types.ts`, `server\src\db\model-pricing.ts`.